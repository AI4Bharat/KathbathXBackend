{
  "name": "@karya/validator",
  "version": "1.0.0",
  "description": "This package implements a module that converts a schema specification to db functions, TS type specifications, etc.",
  "private": true,
  "license": "MIT",
  "main": "dist/Index",
  "types": "dist/Index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "typescript": "4.2.4"
  },
  "dependencies": {
    "@karya/common": "^2.0.0",
    "@karya/core": "^2.0.0"
  }
}